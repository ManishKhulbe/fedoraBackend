version: "3"

services:
  server:
    container_name: fedora-api
    build:
      context: ./
      dockerfile: dockerfile
    environment:
      HOST: 0.0.0.0
    ports:
      - 5001:5001
    volumes:
      - .:/app
    env_file:
      - ./.env
